// Registers @testing-library/jest-dom matchers (toBeInTheDocument, toHaveRole, …)
// and auto-cleans the rendered DOM between tests. Loaded via vitest.config setupFiles.
import '@testing-library/jest-dom/vitest';
